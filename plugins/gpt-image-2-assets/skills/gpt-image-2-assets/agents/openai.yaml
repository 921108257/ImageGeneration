interface:
  display_name: "GPT Image 2 Assets"
  short_description: "Generate professional UI assets for frontend work"
  default_prompt: "Use $gpt-image-2-assets to create a polished, production-ready frontend asset."

policy:
  allow_implicit_invocation: true
